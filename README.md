# Smart Task Management Dashboard

## 📌 Project Overview
A frontend-only task management dashboard built using React.js. 
The application allows users to create, edit, delete, filter, sort, and manage tasks efficiently.

## 🚀 Features
- Add tasks with title, description, priority, and due date
- Edit and delete tasks
- Change task status (To Do, In Progress, Completed)
- Filter by status and priority
- Sort by due date
- Dashboard summary (Total, Completed, Pending)
- Data persistence using localStorage
- Responsive and modern UI with wine & pink theme

## 🛠 Tech Stack
- React.js (Functional Components)
- JavaScript (ES6+)
- CSS
- Browser localStorage API

## 📦 Installation
1. Clone the repository
2. Run `npm install`
3. Run `npm start`

## 🎯 Learning Outcome
This project demonstrates state management, component structure, conditional rendering, filtering, sorting, and localStorage integration in React.